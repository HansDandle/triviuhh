services:
  - type: web
    name: fakeage
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python triviuhh.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
